# TruthRaw launcher artwork integration

The approved source artwork is `strak_tr_logo_met_blauwe_gloed.png` from the implementation session. The APK already has adaptive/round launcher-icon resources wired and a TR vector fallback.

The exact approved raster artwork must be committed through a binary-safe Git path before the fallback is removed. Do not claim the fallback vector is byte/visual-identical to the approved source artwork.
