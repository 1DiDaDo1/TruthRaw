# TruthRaw chat handoff — 2026-09-11

Use this after `START_HERE_NEW_CHAT.md` for fast continuity.

## Where the project is

Promoted `main` remains `514f2f4bde6aba5a6709e176c03b22c3b9aea912`. The most integrated current research state is `research/android-source-bound-color-preview-v0.1-2026-09-11` at `2fdf05ca1bbbc59cd8867df0cae117d1eec92d51`. The latter is green research integration, not automatically main-promoted code.

The documentation refresh lives on `docs/project-handoff-2026-09-11` and is based on that exact research state.

## Documentation rule for every continuation

Every substantive change must obey `docs/DOCUMENTATION_SYNC_POLICY_2026-09-11.md` before the work cycle is considered complete. That means checking/updating the affected module README or state overlay and updating the global current handoff whenever heads, CI evidence, proof boundaries, architecture, scientific authority, or the recommended next action changed.

Historical/sealed/versioned README material is not rewritten for cosmetics. Use a current overlay/index when preservation matters. A code implementation may be validated before its documentation overlay, but it must not be presented as the new global handoff until documentation synchronization is complete.

## Scientific center

TruthRaw reconstructs from sealed source measurements. The scientific baseline is single-frame, measured-preserving v4.7i. Measured/reconstructed/counterfactual/appearance/projection roles never collapse into one another. Zero-line is a reference gauge. Scientific Master is separate from export/preview. Resource capability cannot alter evidence authority.

## Current Main-House execution

Tile-native source and two-pass streaming contracts exist. The old 2026-09-10 statement that memory migration is the next task is superseded. Room ABI/Building Runtime can admit bounded source/process/sink resources while preserving one Backplane/lineage.

## Current professional RAW strategy

Do not feed LibRaw directly into scientific truth authority. The architecture is:

`source -> probe/router -> adapter -> ingress classifier -> Gatehouse -> decode/audit -> persisted handoff -> decoder detach -> decoded IRawTileSource -> Main House`.

Native-certified strict DNG bypasses Gatehouse.

Current LibRaw work is compatibility/probe and borrowed-fd infrastructure. There is not yet a certified vendor pixel-decode matrix. The next real decode adapter must confine `unpack()` to the Gatehouse, report full-frame memory honestly and default to ResearchOnly until named sample-equivalence proof exists.

## Current Android preview strategy

The historical v0.2 gray CFA source proxy remains diagnostic. The new active research route binds exact source bytes by SHA-256, derives source-bound DNG color metadata, performs Main-House streaming reconstruction and emits a bounded sRGB appearance preview/JPEG.

The visible result is `SOURCE_BOUND_APPEARANCE_PREVIEW` / `SOURCE_METADATA_BOUND`. It is intentionally **not** finalized Scientific Preview authority because deterministic Scientific Master identity/phase-2 Backplane finalization is still open.

## Exact newest Android proof

Current research head `2fdf05ca1bbbc59cd8867df0cae117d1eec92d51` has successful Android Source-Bound Color Preview workflow run `34614165763`, including contract, upstream integrity, source/color authority boundary, no-camera-permission, ARM64 NDK/CMake/JNI assembly and artifact upload.

This does not prove a real Honor/MotionCam run.

## Gatehouse transfer proof

- Gatehouse Runtime current PR head `8d417d2c8bda7eb011b68c5acadac0dc01405cfe`: validated research matrix.
- Decoded Measurement Handoff `b4c25c267867769313ce7c79be1fb14968a8b13f`: run `34599504412` success.
- Decoded Measurement Tile Source `0edca8edab8afed9aad83b133c8f578e8b7860e4`: run `34599879528` success.
- Decoded Measurement Main-House E2E branch `b45b32f1302df0355cf733bf0d42fc6eca90d561`: no current-head workflow found; do not call it validated.

## Open PR stack audited

PRs #9 through #20 are open/draft research candidates. PR #21 is the project-wide 2026-09-11 documentation/handoff refresh above the integrated Android research head. Before acting on any PR, fresh-check state/head/CI because this file is a dated snapshot.

## Best next technical sequence

1. physical Honor + real MotionCam DNG execution/evidence for the newest Android route;
2. deterministic Scientific Master serialization/hash;
3. Technical Backplane phase-2 finalization and finalized Scientific Preview gate;
4. actual LibRaw Decode Adapter confined to Gatehouse;
5. real vendor RAW corpus/certification matrix;
6. CI-complete decoded-measurement Main-House E2E;
7. only then review promotion/merge ordering.

## Never do

- never finish substantive project work without the required documentation synchronization;
- never force-push;
- never merge/promote merely because a branch exists;
- never call extension recognition scientific format support;
- never turn a failed run into historical success;
- never use parser sentinel/identity matrix as scientific color;
- never let Gatehouse create a second evidence/master/zero-line;
- never use JPEG/preview bytes as evidence;
- never let APK/GCam determine TruthRaw science;
- never claim `FULL_PHYSICAL` without the missing independent physical calibration/evidence.