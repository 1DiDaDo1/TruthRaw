# DNG Projection v0.1 Current-Head Validation Rerun

This record intentionally triggers the DNG projection validation workflow on the current Android-integrated branch head after the JNI status-code mapping fix.

No scientific, projection, color, evidence, or rendering semantics are changed by this file.

Promotion remains blocked until GCC Release, Clang Release, Clang ASan/UBSan, and Android arm64 integration are all independently green on a descendant of the exact implementation head.
