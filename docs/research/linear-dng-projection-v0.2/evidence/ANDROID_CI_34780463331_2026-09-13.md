# Linear DNG Projection v0.2 — Android arm64 CI evidence

Date: 2026-09-13

Workflow: `Android Linear DNG Export v0.2 Restore`

GitHub Actions run: `34780463331`

Built head SHA: `e7bcb103215f97bc0ca68e526bd07453ade46a1c`

Branch: `research/restore-rgb-linearraw-output-v0.2-workbase-2026-09-13`

## Result

Overall build result: **SUCCESS**

Both jobs completed successfully:

- `Host Linear DNG v0.2 writer` — SUCCESS
- `Android arm64 Linear DNG v0.2 restore APK` — SUCCESS

The Android job completed:

- export-boundary/static authority checks;
- Java 17 setup;
- Android SDK / build-tools / CMake / NDK setup;
- Gradle debug assembly;
- arm64 native build;
- JNI symbol verification;
- v0.2 native string/path verification;
- APK artifact upload.

## Exact APK identity

Built APK:

`app-debug.apk`

Exact byte length:

`4335889`

Exact SHA-256:

`93e51245e0950c5c3140b83f2f3429d2f52ad48adcd5630409134e4c430b5654`

Application build identity in source:

- `versionCode = 6`
- `versionName = 0.6-linear-dng-restore`
- ABI: `arm64-v8a`

## Artifact identity

GitHub artifact:

`truthraw-linear-dng-v0.2-restore-debug-arm64`

Artifact ID:

`10325007080`

Artifact ZIP bytes:

`1380123`

Artifact ZIP SHA-256:

`6db62333d1100cc4d6cabb928b3e986d39bed7a0d02421c95d007b738293f41f`

The downloaded artifact ZIP was independently unpacked after the run and reproduced the exact APK byte length and APK SHA-256 listed above.

## Native verification recorded by CI

The built arm64 shared library contains both the historical v0.1 writer and the new v0.2 wrapper symbol, while the Android bridge is wired to v0.2.

Verified strings/symbols include:

- `Java_com_truthraw_adaptiveui_LinearDngNativeBridge_exportFinalizedLinearDng`
- `truthraw::linear_dng_projection::v0_2::write_finalized_linear_dng(...)`
- `linear_dng_v0_2_finite_2x_representation_wrapper`
- `reconstructed RGB exceeds validated 2x LinearRaw compatibility window`

CI also records:

- `linear_dng_authority=COMPATIBILITY_PROJECTION_ONLY`
- `scientific_authority_promotion=FORBIDDEN`
- `finite_window=2x`
- `baseline_exposure_ev=+1`
- `source_camera_identity=PRESERVED`
- `linear_dng_photometric=LinearRaw_34892`
- `linear_dng_storage=UNCOMPRESSED_UINT16_RGB`
- `full_scientific_master_materialization=FORBIDDEN`
- `truthraw_execution_backend=CPU_NDK_REFERENCE_NO_VULKAN`

## Preserved warning

The frozen canonical v4.7i `core.cpp` still emits the known Clang `-Wmisleading-indentation` warning. It remains visible and non-fatal only for that frozen translation unit. This is not rewritten merely to make the log clean.

## Scope of proof

This CI proves the exact v0.2 code at built head `e7bcb103...` compiles and packages as an arm64 Android APK and that the expected v0.2 native export path is inside the APK.

It does **not** yet prove:

- physical execution on the Honor BKQ-N49;
- that the produced DNG opens/edits correctly in Lightroom;
- Android gallery/piex embedded-preview discovery;
- the historical multi-IFD JPEG preview restoration (not yet implemented in this APK);
- exact DNG SDK/LibRaw interoperability of a v0.2 file produced on-device;
- FULL_PHYSICAL color or independent lens/camera calibration.

Those remain separate gates.
