# Linear DNG Projection v0.2 — host CI evidence

Date: 2026-09-13

Workflow: `Linear DNG Projection v0.2`

GitHub Actions run: `34780375681`

Head SHA: `c3bdf39c5f48e576d97446074ead19705bef688c`

Branch: `research/restore-rgb-linearraw-output-v0.2-workbase-2026-09-13`

## Result

Overall conclusion: **SUCCESS**

All three configured jobs completed successfully:

- GCC Release — SUCCESS
- Clang Release — SUCCESS
- Clang ASan/UBSan — SUCCESS

The configure, build and test steps passed in each job. The sanitizer job ran with AddressSanitizer and UndefinedBehaviorSanitizer enabled and completed successfully.

## What this proves

For the exact head above, the v0.2 host test executable compiles and passes under GCC and Clang, including the sanitizer configuration.

The regression test covers the v0.2 finite-window/camera-identity contract, including:

- scene-linear reconstructed RGB value `1.5` preserved without high clipping through the 2x representation window;
- `BaselineExposure = +1 EV` representation contract;
- source `HONOR` / `BKQ-N49` / `BKQ-N49-HONOR-HONOR` identity restoration;
- fail-closed rejection for reconstructed scene values above the validated 2x window.

## What this does not prove

This host CI does not prove:

- Android/NDK packaging;
- physical Honor execution;
- Lightroom compatibility;
- embedded preview availability;
- LibRaw or DNG SDK interoperability of the new exact Android-produced bytes;
- FULL_PHYSICAL color or independent camera/lens calibration.

Those gates remain separate.
