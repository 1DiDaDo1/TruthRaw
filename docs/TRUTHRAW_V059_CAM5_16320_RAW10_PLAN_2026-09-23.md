# TruthRaw Camera-5 16320x12288 RAW10 probe v0.59 — 2026-09-23

Status: **ISOLATED EXPERIMENT / BUILD PENDING OR RUNNING / FROZEN v0.84.3 UNCHANGED**

Branch:

`test/cam5-16320x12288-raw10-v059`

Frozen production/scientific anchor remains:

`integration/truthraw-suite-v0-84-3-float32-full-colour-scientific-master`
at `172a100786eb18d4b08564bbfb025a44f42cfa1e`.

## Why v0.59 exists

The current evidence chain now contains three independently useful public Camera2 observations on physical Camera 5:

1. v0.56c RAW_SENSOR 8160x6144:
   - app-visible envelope = 100,270,080 bytes;
   - meaningful U16 extent = exactly 25,067,520 bytes;
   - that byte count uniquely matches advertised STANDARD RAW_SENSOR 4080x3072.

2. v0.57 RAW10 8160x6144, two fresh captures:
   - app-visible envelope = 62,914,560 bytes;
   - meaningful front region = 15,728,640 bytes;
   - row interpretation = 5100 packed RAW10 pixel bytes + 20 zero padding bytes, repeated for 3072 rows;
   - remaining envelope tail is zero;
   - working classification:
     `APP_VISIBLE_4080x3072_PACKED_RAW10_PAYLOAD_EMBEDDED_IN_8160x6144_CAMERA2_ENVELOPE`.

3. v0.58 STANDARD RAW10 4080x3072 control:
   - sealed source SHA-256:
     `399eb12ce618029e20f86ab71b840cc54c85fb08e8ffb2d16a684ff7894cec45`;
   - bytes = 15,728,640;
   - rowStride = 5120;
   - packed pixel bytes/row = 5100;
   - row padding = 20;
   - exactly 3072 rows;
   - this independently reproduces the same row geometry seen at the front of v0.57.

Therefore the strongest current public-Camera2 interpretation is that the tested 8160 RAW10 route carries the ordinary 4080x3072 packed-RAW10 row layout inside a larger declared/envelope geometry.

This remains an app-visible Camera2 observation. It is not native photodiode/ADC geometry proof.

## v0.59 question

The remaining clean public-RAW control is:

**What byte topology does a 16320x12288 physical-Camera-5 RAW10 MAXIMUM_RESOLUTION request actually return?**

v0.59 therefore requests exactly:

`logical 0 -> physical 5 -> RAW10 16320x12288 -> MAXIMUM_RESOLUTION output -> physical-scoped MAX SENSOR_PIXEL_MODE`

The proven asymmetric v0.14/v0.57 request rule is preserved:

- no logical/global SENSOR_PIXEL_MODE write;
- physical Camera-5 output binding;
- output declares MAXIMUM_RESOLUTION;
- physical-scoped still request;
- physical SENSOR_PIXEL_MODE=MAXIMUM_RESOLUTION is attempted even when the physical override key is not advertised;
- returned SENSOR_PIXEL_MODE remains advisory until after source sealing.

## Evidence-first contract

v0.59 is analysis-only.

It may record and export:

- exact Image dimensions;
- physical Camera-5 TotalCaptureResult identity;
- Image timestamp == physical SENSOR_TIMESTAMP;
- rowStride;
- pixelStride;
- accessible plane bytes;
- packed bytes per declared row;
- declared row padding;
- SHA-256 of the complete app-visible Image.Plane;
- capture ISO/exposure/focus/NR/edge/binning telemetry;
- black/white/noise/color/lens telemetry as observation-only metadata.

It must not:

- interpret packed RAW10 as U16 RAW_SENSOR;
- create a DNG from the packed buffer;
- admit a Scientific Master;
- admit TruthNegative;
- upgrade calibration authority;
- claim native 200MP ADC geometry;
- treat a 16320x12288 envelope as proof that all 200,540,160 declared samples are populated.

## Vision binding

This experiment changes only the **Foundation / Measurement evidence boundary**.

It does not alter the higher TruthRaw architecture:

`sealed source evidence -> measurement/de-ISP -> Scientific Master -> Dynamic Authority/uncertainty -> Open Scene / Free Scientific Space -> TruthNegative/projections -> appearance/export`

Permanent laws remain:

- measured, reconstructed, censored/unknown, counterfactual and appearance stay separate;
- BlackLevel is a measurement offset, never the TruthRange zero-line;
- WhiteLevel is a censoring boundary, never the top of the reconstructed world;
- `T = log2(L/L0)` uses a scene gauge `L0`, not sensor black;
- Float32/F64 headroom is representational freedom, not extra photons;
- source-bound colour metadata is not independent physical colour calibration;
- light/CCT does not prove SPD, direction, mixed-light state or geometry;
- restoration may compensate loss but may not overpaint valid measured support;
- detail/acutance may not invent unsupported spatial information;
- counterfactual relighting and HDR presentation create no new evidence;
- one physical frame remains one independent evidence source.

Short form:

**Representation can exceed the source. Knowledge claims cannot exceed the evidence.**

## Decision gate after device capture

After a real v0.59 capture, compare its complete sealed RAW10 plane against the v0.58 standard control and v0.57 8160 probe.

Primary checks:

- total plane bytes;
- rowStride;
- packed pixel bytes/row;
- row padding;
- first/last non-zero byte;
- non-zero row population;
- whether the meaningful region is again byte-compatible with a 4080x3072 / 5120-byte-row RAW10 plane;
- whether any additional non-zero region carries independent spatial content.

Only the bytes may decide the classification.

If v0.59 again collapses to the same 4080x3072 packed domain, public Camera2 high-resolution RAW on this tested route should be treated as practically exhausted, and further high-resolution acquisition research should focus on the Honor/QTI vendor remosaic path rather than larger public RAW envelopes.
