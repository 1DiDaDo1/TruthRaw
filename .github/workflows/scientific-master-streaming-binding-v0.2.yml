name: Scientific Master Streaming Binding v0.2

on:
  push:
    branches:
      - 'research/bit-exact-read-optimization-v0.1-*'
    paths:
      - 'docs/research/scientific-master-streaming-binding-v0.2/**'
      - '.github/workflows/scientific-master-streaming-binding-v0.2.yml'
      - 'docs/research/scientific-master-streaming-binding-v0.1/**'
      - 'docs/research/scientific-master-digest-v0.1/**'
      - 'docs/research/truthrange-latent-binding-v0.2/**'
      - 'docs/research/full-frame-streaming-v0.1/**'
  pull_request:
    paths:
      - 'docs/research/scientific-master-streaming-binding-v0.2/**'
      - '.github/workflows/scientific-master-streaming-binding-v0.2.yml'

permissions:
  contents: read

jobs:
  native:
    name: ${{ matrix.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: GCC Release
            cxx: g++
            build_type: Release
            sanitizers: 'OFF'
          - name: Clang Release
            cxx: clang++
            build_type: Release
            sanitizers: 'OFF'
          - name: Clang ASan-UBSan
            cxx: clang++
            build_type: Debug
            sanitizers: 'ON'
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        run: >-
          cmake -S docs/research/scientific-master-streaming-binding-v0.2
          -B build/scientific-master-streaming-binding-v0.2
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
          -DCMAKE_CXX_COMPILER=${{ matrix.cxx }}
          -DTRUTHRAW_SMSB2_ENABLE_SANITIZERS=${{ matrix.sanitizers }}
      - name: Build
        run: cmake --build build/scientific-master-streaming-binding-v0.2 --parallel 2
      - name: Test
        run: ctest --test-dir build/scientific-master-streaming-binding-v0.2 --output-on-failure
