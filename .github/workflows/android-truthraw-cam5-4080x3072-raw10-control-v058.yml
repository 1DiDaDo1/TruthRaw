name: TruthRaw Camera5 4080x3072 RAW10 control v0.58

on:
  workflow_dispatch:
  push:
    branches: [test/cam5-4080x3072-raw10-control-v058]
    paths:
      - 'suite_android/app/src/main/java/com/truthraw/adaptiveui/FotoGraaf200MpStagedActivity.kt'
      - '.github/workflows/android-truthraw-cam5-4080x3072-raw10-control-v058.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Verify isolated STANDARD RAW10 control invariants
        run: |
          set -euo pipefail
          SRC='suite_android/app/src/main/java/com/truthraw/adaptiveui/FotoGraaf200MpStagedActivity.kt'
          grep -F 'private const val TARGET_W = 4080' "$SRC"
          grep -F 'private const val TARGET_H = 3072' "$SRC"
          grep -F 'ImageReader.newInstance(TARGET_W, TARGET_H, ImageFormat.RAW10, 1)' "$SRC"
          grep -F 'STANDARD_RAW10_4080x3072_NO_MAX_PIXEL_MODE' "$SRC"
          grep -F 'outputMaximumResolutionModeDeclared", false' "$SRC"
          ! grep -F 'output.addSensorPixelModeUsed' "$SRC"
          ! grep -F 'setPhysicalCameraKey(' "$SRC"
          grep -F 'physicalFrameCount", 1' "$SRC"
          grep -F 'independentEvidenceCount", 1' "$SRC"
          grep -F 'scientificMasterCreationAllowed", false' "$SRC"

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install pinned Android SDK and NDK
        run: |
          set -euo pipefail
          SDKMANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          if [ ! -x "$SDKMANAGER" ]; then SDKMANAGER="$(command -v sdkmanager)"; fi
          yes | "$SDKMANAGER" --licenses >/dev/null || true
          "$SDKMANAGER" 'platforms;android-35' 'build-tools;35.0.0' 'cmake;3.22.1' 'ndk;27.2.12479018'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Assemble arm64 debug APK
        working-directory: suite_android
        run: gradle --no-daemon :app:assembleDebug

      - name: Verify APK
        run: |
          set -euo pipefail
          APK='suite_android/app/build/outputs/apk/debug/app-debug.apk'
          test -s "$APK"
          echo "apk_bytes=$(stat -c%s "$APK")"
          echo "apk_sha256=$(sha256sum "$APK" | awk '{print $1}')"

      - uses: actions/upload-artifact@v4
        with:
          name: truthraw-cam5-4080x3072-raw10-control-v058-debug-arm64
          path: suite_android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
